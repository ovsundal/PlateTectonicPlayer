## Codebase Patterns

### Git Worktree Layout
- **Where**: root of repo
- **Pattern**: The project git repo is at `C:/Users/ove.sundal/WebstormProjects/PlateTectonicPlayer/`. The archon worktree (`archon/thread-ae2b8fff`) is a git worktree of that same repo. All source files must be created in the WORKTREE path (`.archon/workspaces/.../worktrees/archon/thread-ae2b8fff/`), NOT in WebstormProjects directly, so they are on the correct branch for commits.
- **Example**: `git add package.json` from the worktree path stages to `archon/thread-ae2b8fff`

### npm Commands
- **Where**: project root
- **Pattern**: Run `npx tsc --noEmit`, `npx eslint src`, `npx vite build` from the WORKTREE path (cd first). Shell CWD resets between Bash commands so always use `cd /path && command` in a single command.

### deck.gl GlobeView in v9
- **Where**: `src/components/Globe.tsx`
- **Pattern**: In deck.gl v9, `GlobeView` is exported as `_GlobeView` (experimental). Import as `import { _GlobeView as GlobeView } from 'deck.gl'`. Using `GlobeView` directly fails at build time (not exported from dist/index.js).

---

## 2026-04-14 — US-001: Scaffold Vite + React + TypeScript project

**Status**: PASSED
**Files changed**:
- `package.json` — created with scripts: dev, build, preview, typecheck, lint
- `tsconfig.json` — project references to tsconfig.app.json and tsconfig.node.json
- `tsconfig.app.json` — strict mode, jsx: react-jsx, bundler moduleResolution
- `tsconfig.node.json` — for vite.config.ts
- `vite.config.ts` — @vitejs/plugin-react configured
- `index.html` — references /src/main.tsx
- `src/main.tsx` — renders <App /> into #root
- `src/App.tsx` — placeholder div component
- `eslint.config.js` — TypeScript + react-hooks + react-refresh rules

**Acceptance criteria verified**:
- [x] package.json exists with scripts: dev, build, preview, typecheck, lint
- [x] tsconfig.json exists with strict mode enabled and JSX set to react-jsx
- [x] vite.config.ts exists with @vitejs/plugin-react configured
- [x] index.html exists referencing src/main.tsx
- [x] src/main.tsx renders <App /> into #root
- [x] src/App.tsx exists and renders a placeholder <div>
- [x] `npx tsc --noEmit` passes with zero errors
- [x] `npx vite build` completes without errors

**Learnings**:
- Files must go in the WORKTREE path, not in C:/Users/ove.sundal/WebstormProjects/PlateTectonicPlayer/ (which is on branch main)
- Shell CWD resets between Bash commands — always `cd /path && command`
- The worktree `.git` file points to `WebstormProjects/PlateTectonicPlayer/.git/worktrees/thread-ae2b8fff` confirming they share one git repo

---

## 2026-04-14 — US-002 through US-010: Full implementation

**Status**: ALL PASSED

**US-002**: Add deck.gl and render static GlobeView
- `src/components/Globe.tsx` — DeckGL + _GlobeView, layers + initialViewState props, #1a2744 background
- Key learning: `GlobeView` is `_GlobeView` in deck.gl v9

**US-003**: Create preprocess_muller.py data pipeline
- `scripts/preprocess_muller.py` — CLI using click, geopandas, auto-detects age column, writes plates_{N}Ma.geojson
- `scripts/requirements.txt` — geopandas, fiona, click

**US-004**: Define TypeScript types for plate data
- `src/types/plates.ts` — PlateProperties, PlateFeature, PlateFeatureCollection, AnimationState, AGE_MIN/MAX/STEP

**US-005**: Implement usePlateData hook (fetch + cache)
- `src/hooks/usePlateData.ts` — module-level Map cache, prefetches next 2 steps, error-safe

**US-006**: Render GeoJsonLayer plate polygons on globe
- `src/components/Globe.tsx` — added data prop, GeoJsonLayer with stroked outlines
- `src/App.tsx` — updated to pass data from usePlateData(0)

**US-007**: Implement useAnimation hook (rAF play/pause)
- `src/hooks/useAnimation.ts` — rAF loop, play/pause/seek, auto-stops at AGE_MAX=750, exposes speed/setSpeed

**US-008**: Create TimelineControls component
- `src/components/TimelineControls.tsx` — Play/Pause button, range scrubber, age label, speed selector

**US-009**: Wire App.tsx
- `src/App.tsx` — useAnimation + usePlateData + Globe + TimelineControls + AgeLabel fully wired

**US-010**: Polish
- `src/components/AgeLabel.tsx` — prominent age display top-center
- `src/components/Globe.tsx` — filled plates with PLATEID1 hash colors (alpha 80)
- `src/components/TimelineControls.tsx` — 10/30/100 Ma/s speed selector
- `src/hooks/useAnimation.ts` — exposes speed + setSpeed

**All criteria verified**: `npx tsc --noEmit` ✓, `npx eslint src` ✓, `npx vite build` ✓

---
